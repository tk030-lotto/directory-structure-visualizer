# ChatGPTやClaudeへの説明に便利。ディレクトリ構造をMarkdownツリー化するWebツール

🌐 **GitHub Pages (Webアプリ版)**: https://tk030-lotto.github.io/directory-structure-visualizer/

---

ChatGPTやClaudeなどの生成AIにプログラミングの相談をする際、「プロジェクトの全体構造」をプロンプトに含めたい場面がよくあります。

しかし、手動でフォルダやファイルの木構造（`├──` や `└──`）を書くのは手間がかかり、不要な `node_modules` や `.git` などのファイルが混ざってしまうこともあります。

そこで、ローカルのフォルダをドラッグ＆ドロップするだけで、AIにそのまま貼り付けられるMarkdown形式のツリー構造を即座に生成するWebツールを開発・利用しています。

---

## 🛠️ 主な機能と特徴

### 1. フォルダをドラッグ＆ドロップするだけで即座に解析
ブラウザ上に対象のプロジェクトフォルダをドロップ（または選択）するだけで、瞬時に階層構造を読み込みます。

### 2. 不要なフォルダ・ファイルの自動除外
デフォルトで `node_modules`, `.git`, `.DS_Store`, `dist` などを自動除外します。また、任意の除外パターン（`*.log` や特定のフォルダ名）をカスタム指定することも可能です。

### 3. ワンクリックでMarkdownコピー ＆ ファイル保存
生成されたツリーテキストはボタン一つでクリップボードにコピーできます。そのままAIのプロンプトに貼り付けることができ、必要に応じて `.md` ファイルとしての保存も可能です。

### 4. ブラウザ内で処理が完結（ローカル完結で安心）
選択したファイルやフォルダのデータは外部サーバー等へ送信されず、すべてお使いのブラウザ内（ローカル）で処理されるため、安心して利用できます。

---

## 📖 使い方（3ステップ）

1. **フォルダを選択**: 画面中央のエリアにフォルダをドラッグ＆ドロップ（または選択ボタンから指定）します。
2. **オプション調整**: 必要に応じて除外条件やツリー記号のスタイル（Unicode / ASCII）を調整します。
3. **コピーしてAIへ**: 右側の出力エリアから「コピー」ボタンを押し、ChatGPTやClaude等のプロンプトに貼り付けます。

---

## 🔗 リンクとリポジトリ

- 🌐 **Webで試す (GitHub Pages)**: https://tk030-lotto.github.io/directory-structure-visualizer/
- 🐙 **GitHub リポジトリ**: https://github.com/tk030-lotto/directory-structure-visualizer

#プログラミング #ChatGPT #Claude #Web開発 #個人開発 #エンジニア #React #TypeScript #便利ツール
